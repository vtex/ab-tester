import { HttpClientFactory, IODataSource } from './IODataSource';
export declare class Logger extends IODataSource {
    protected service: string;
    protected httpClientFactory: HttpClientFactory;
    debug: (message: any, subject?: string) => Promise<void>;
    info: (message: any, subject?: string) => Promise<void>;
    warn: (message: any, subject?: string) => Promise<void>;
    error: (error: any, details?: Record<string, any> | undefined, subject?: string) => Promise<void>;
    sendLog: (subject: string, message: any, level: string) => Promise<void>;
}
export interface ErrorLog {
    code: string;
    message: string;
    response?: {
        status: number;
        data: string;
        headers: Record<string, string>;
    };
    [key: string]: any;
}
