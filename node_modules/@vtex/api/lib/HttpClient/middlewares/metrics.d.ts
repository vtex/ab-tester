import { MetricsAccumulator } from '../../metrics/metricsAccumulator';
import { MiddlewareContext } from '../context';
export declare const metricsMiddleware: (metrics: MetricsAccumulator) => (ctx: MiddlewareContext, next: () => Promise<void>) => Promise<void>;
