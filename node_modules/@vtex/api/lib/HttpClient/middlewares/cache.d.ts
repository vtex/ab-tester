import { AxiosRequestConfig, AxiosResponse } from 'axios';
import { CacheLayer } from '../../caches/CacheLayer';
import { MiddlewareContext } from '../context';
export declare const cacheMiddleware: ({ cacheStorage, segmentToken }: {
    cacheStorage: CacheLayer<string, Cached>;
    segmentToken: string;
}) => (ctx: MiddlewareContext, next: () => Promise<void>) => Promise<void>;
export interface Cached {
    etag: string;
    expiration: number;
    response: Partial<AxiosResponse>;
}
export declare type CacheableRequestConfig = AxiosRequestConfig & {
    url: string;
    cacheable: boolean;
};
