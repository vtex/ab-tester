import { IODataSource } from './IODataSource';
export declare class Billing extends IODataSource {
    protected service: string;
    protected httpClientFactory: import("./IODataSource").HttpClientFactory;
    contractStatus: () => Promise<ContractStatus>;
    sendMetric: (metric: BillingMetric) => Promise<void>;
}
export declare enum ContractStatus {
    ACTIVE = "active_contract",
    INACTIVE = "inactive_contract",
    NO_CONTRACT = "no_contract"
}
export interface BillingMetric {
    value: number;
    unit: string;
    metricId: string;
    timestamp?: number;
}
